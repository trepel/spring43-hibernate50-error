# spring43-hibernate50-error

To run, execute `mvn clean test` in terminal.

This is the reproducer for incompatibility of Spring 4.3.0.RELEASE and Hibernte 5.1.0.Final.

Go to pom.xml and change the Hibernate version to 5.2.0.Final to see this working.

